# -----------------------------------------------
# Dev Container
# -----------------------------------------------
# コンテナ名（任意。省略時はデフォルト名が使われる）
CONTAINER_NAME=kaggle_06_predicting_f1_pit__stops_dev

# -----------------------------------------------
# MLflow（ローカルファイルベース）
# -----------------------------------------------
# mlruns/ をそのままトラッキングURIとして使う（サーバー不要）
MLFLOW_TRACKING_URI=mlruns
MLFLOW_EXPERIMENT_NAME=kaggle_06_predicting_f1_pit_stops

# -----------------------------------------------
# Streamlit
# -----------------------------------------------
STREAMLIT_PORT=8501

# -----------------------------------------------
# JupyterLab
# -----------------------------------------------
JUPYTER_PORT=8888

# -----------------------------------------------
# 外部API（使用する場合のみ設定・値は .env に記載）
# -----------------------------------------------
# OPENAI_API_KEY=
# OTHER_API_KEY=
